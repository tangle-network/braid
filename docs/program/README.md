# Program documents

Release-completion program documents for Braid, seeded on the `factory/release-completion`
branch from the verified baseline (main `3d7b79f` = tag `v0.1.3` = npm `0.1.3`).

- `braid-VISION.md` — the release-completion VISION with numbered priorities P1–P6.
- `braid-PROGRAM-MAP.md` — the program map: verified baseline, node assignments, and the
  final `swe-final-integration-pr` node that flips the draft PR to ready.

Both were reconstructed from `README.md`, `docs/08-verification.md`, and
`docs/09-delivery-plan.md` evidence because the round-1 originals were not handed to this arm.
Completion authority remains `docs/08-verification.md` and `docs/09-delivery-plan.md`
(`AGENTS.md:71`).
