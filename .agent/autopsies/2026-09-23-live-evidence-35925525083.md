# LIVE-07 completed-run focus crash

The protected `live-tangle` run `35925525083` tested Braid `f406f65dd2b5248403e806b5d107ba2fe9636cc6` from a `0.3.0` package tarball.
Its retained two-run proof failed in `branch-a.complete` while opening completed run A's transcript.
The evidence is `/tmp/braid-live-35925525083/logs/check-live-tangle-attempt-1-evidence-00001-a4da9d1483db00fb4f456c8d39f57cc29914af8797647d5ae94ab73db46d27f6.json`.

The last accepted events for run A were `run.finished` at journal sequence 99 and `effect.upserted` at 100.
The terminal then exited with `StorageError` code `TERMINAL_RUN_MUTATION` for run A.
The script was focusing A from the activity browser so it could inspect A's transcript.
The selected `model-processing` activity item was an earlier source event, not evidence of new provider output after completion.
Run B had been cancelled, and cleanup confirmed deletion of both sandboxes with an active resource delta of zero.

`BraidApplication.focusRun` records `run.focused` with the selected run's identifier.
SQLite interprets that identifier as a terminal run mutation because `run.focused` was absent from its post-terminal sideband list.
This rejects local navigation even though it cannot change provider output.

An encrypted SQLite application test reproduced the same error with two completed conversations before the fix.
Adding only nonterminal `run.focused` to the sideband list made that flow pass and restored A's focus after restart.
The existing MemoryStorage and SQLite terminal-policy tests still reject late output, usage, and a second final event.
This is an execution failure of the Braid package, so the protected LIVE-07 result remains failed until a new exact-candidate run passes.
