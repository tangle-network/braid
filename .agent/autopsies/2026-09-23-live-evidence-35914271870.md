# Live Evidence 35914271870 autopsy

## Run and expected result

Protected workflow [35914271870](https://github.com/tangle-network/braid/actions/runs/35914271870) ran on 2026-09-23 against commit `fd785c9b651fd9116d8badbfcc38140ba95baa78`.
It used candidate Release run 35850968583 and the 0.3.0 tarball with SHA-256 `2a2948873b35888e1c2f6f36ec465b154f0596ef12461572d27bbde39ea9aff0`.
The `release-live` secret had been updated at 20:11:05Z to repair the previous missing settings.
The expected result was passing LIVE-06 through LIVE-10 receipts from the installed candidate.

## Raw result and accounting

The diagnostic artifact is GitHub artifact 10774431797, `braid-live-evidence-diagnostic-fd785c9b651fd9116d8badbfcc38140ba95baa78`.
Its `checks.json` has six failed records: `live-tangle` and LIVE-06 through LIVE-10.
All six records point to one `pnpm test:live:tangle` execution and the same exit code 1.
That execution completed one production inference turn, then failed before producing any row receipt.
The exact error was `turn run-63a53ac3-3bd6-4919-9105-9c157cec6860 did not report cancellation as unavailable`.
LIVE-07 through LIVE-10 were not executed; the collector assigned their failed records from the shared command failure.
The diagnostic contains no live-resource or cleanup entry because the Sandbox checks were not reached.
An empty diagnostic resource list does not establish an account-wide zero-resource census.

## Cause and checks

`runHeadlessTurn()` reads the public JSONL `HeadlessState` run after the turn completes.
That public run projects status and usage but omits the internal `capabilities` field.
`runInference()` read `normal.run.capabilities.controls.cancel`, so the value was undefined.
It then called `verifyUnavailableCancellation()`, which required the same missing field to equal false and failed before sending a cancel request.

| Explanation | Discriminating observation | Result |
| --- | --- | --- |
| The repaired credential was still unusable | The protected turn reached `completed` and passed the exact marker check before the cancellation assertion. | Rejected for LIVE-06 admission and inference. |
| Tangle inference could not complete | The same completed turn reached the post-turn assertion. | Rejected for this run. |
| The provider contradicted a declared cancellation capability | The failed assertion ran before a cancellation request. | Not tested by the protected run. |
| The proof read a field absent from the public state | A packed 0.3.0 run produced a completed public run with no `capabilities`; its matching send admission reported `controls.cancel=false`. | Confirmed. |
| The candidate identity changed | The downloaded package proof and endorsement matched the workflow commit and tarball digest. | Rejected. |

The local packed diagnostic used the existing `braid-prod` credential without writing its value to a profile or artifact.
It printed only run shape and boolean capability fields.
The fixed source then ran LIVE-06 alone against the same candidate tarball and local Tangle route.
That run passed, with `cancellationStatus=reported-unavailable` and `cancellationResponseCode=UNKNOWN_RUN`.
The other cloud rows were explicitly skipped in that local diagnostic.

## Decision

This was a release-proof execution bug, not evidence that LIVE-07 through LIVE-10 failed in production.
Read cancellation support from the send acknowledgement's matching admission receipt.
Reject a missing, mismatched, or malformed admission instead of guessing the capability.
Run a new candidate and protected Live Evidence after this fix merges.
Publication remains held for the separate LIVE-08 cloud interaction proof gap.
