# Live Evidence 35918839117 autopsy

## Run and expected result

Protected [Live Evidence 35918839117](https://github.com/tangle-network/braid/actions/runs/35918839117) ran against commit `7116457f7d3470ba97626ca3673d4c44c2fd72a2` and candidate [Release 35918045684](https://github.com/tangle-network/braid/actions/runs/35918045684).
The candidate passed package and platform checks.
The protected run was diagnostic because the separate LIVE-08 cloud interaction fix was still under review.
It was expected to exercise the repaired LIVE-06 path and reveal the next real LIVE-07 condition.

## Observed result

The diagnostic artifact is GitHub artifact 10776667623, downloaded to `/tmp/braid-live-35918839117` on the operator host.
The shared `pnpm test:live:tangle` command exited 1 with `Tangle sandbox failed against the configured live path: LIVE-07 multirun proof used an unproven runner`.
The collector assigned that one command failure to six records: `live-tangle` and LIVE-06 through LIVE-10.
Those records are not separate row verdicts.
LIVE-08 through LIVE-10 were not reached.

The retained multirun artifact has `schemaVersion=braid.live-required.multirun.v2`, `status=passed`, `provider.runner=opencode`, and two distinct provider environments.
It records two overlapping active runs and two visible work-strip rows; one run completed, the other was aborted; restart replay kept both event sets stable.
Both exact recorded sandboxes were deleted and confirmed, with `cleanup.exact=true` and `activeResourceDelta=0` for this two-run subproof.
The aggregate receipt was not written because validation threw.
The artifact does not provide a separate account-wide resource census or durable per-row results for the full headline gate.

## Cause and falsification

`runSandbox()` accepted the multirun object directly with `assertMultirunProof()`.
The same validator also accepted the downloaded JSON artifact in a local replay.
`proofReceipt()` then sanitized combined stress and multirun observations before validating the aggregate receipt.
The stress cohort's non-secret `sessionSpend` field matched the broad credential-name pattern for `session`.
The sanitizer collected every string below that metrics object as a literal secret, including `opencode`, and replaced the multirun runner with `[REDACTED]`.
The second validation rejected the redacted runner.
A focused LIVE-07 wiring regression reproduced the same error before the fix and passed afterward while still redacting `providerSessionId`.

| Explanation | Check | Result |
| --- | --- | --- |
| OpenCode was not the runner | The retained artifact records `provider.runner=opencode`, and the standalone validator accepts it. | Rejected for the two-run subproof. |
| The two-run Sandbox execution or cleanup failed | The retained artifact reports passed execution, exact deletion of both recorded resources, and active-resource delta 0. | Rejected for that subproof. |
| Receipt sanitization changed the runner | A local replay with a `sessionSpend` row changes the multirun runner to `[REDACTED]`; the aggregate validator then throws the protected error. | Confirmed. |

## Decision

Publish Braid's known `sessionSpend` metrics under `spend` within the LIVE-07 observation.
Keep generic `sessionSpend` values, nested provider session identifiers, and actual credential fields redacted.
Retain the fixed LIVE-06 through LIVE-10 headline gate and rerun protected evidence on a new exact candidate after the LIVE-07 and LIVE-08 fixes merge.
